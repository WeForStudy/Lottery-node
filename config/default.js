const config = {
  // 启动端口
  port: 1200,

  // 数据库配置
  database: {
    DATABASE: 'lottery',
    USERNAME: 'root',
    PASSWORD: 'lotterygroup',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config
